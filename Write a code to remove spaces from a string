#include<stdio.h>
void main()
{
    char a[100];
    gets(a);
    int i,j;
    for(i=0;a[i]!='\0';i++)
    {
        if(a[i]==32)
        {
            for(j=i;a[j]!='\0';j++)
            {

                a[j]=a[j+1];

            }
            //i--;
        }
    }
    puts(a);
}
