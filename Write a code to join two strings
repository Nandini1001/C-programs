#include<stdio.h>
#include<string.h>
void main()
{
    char a[100],b[100];
    int la,i,j;
    gets(a);
    gets(b);
    la=strlen(a);
    for(i=0;b[i]!='\0';i++)
    {
        a[la+i]=b[i];
    }
    a[la+i]='\0';
    puts(a);
}
